{
  "name": "samuha",
  "version": "1.0.0",
  "packageManager": "pnpm@8.13.1",
  "main": "index.js",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:migrate": "pnpm --filter samuha-backend run prisma:migrate",
    "prisma:generate": "pnpm --filter samuha-backend run prisma:generate",
    "prisma:studio": "pnpm --filter samuha-backend run prisma:studio",
    "prisma:reset": "pnpm --filter samuha-backend run prisma:reset",
    "prisma:deploy": "pnpm --filter samuha-backend run prisma:deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "turbo": "^2.8.1"
  }
}